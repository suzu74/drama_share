<div class="col-xs-12 col-sm-4 border">
  <% if user.image.attached? %>
    <%= image_tag user.image ,class: "profile-image pt-4" %>
  <% else %>
    <%= gravatar_for user, size: 150 %>
  <% end %>
  <h3>
    <%= link_to user.name, user %>
    <small class="text-muted">さん</small>
  </h3>
</div>